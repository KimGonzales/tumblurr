<h1>Post Index</h1>

<% @posts.each do |post| %>
  <div class="post_wrappper">
    <h2 class="title"><%= link_to post.title, post %></h2>
    <p class="date"><%= time_ago_in_words(post.created_at) %></p>
  </div>
<% end %>